interface:
  display_name: "Zotero Declarative Bridge"
  short_description: "Resolve exact collections or apply constrained Zotero transactions"
  default_prompt: "Use $zotero-declarative-bridge to resolve an authorized exact group-library collection key read-only when its internal collection ID is needed, or to preview, apply, and read back this reviewed hash-bound Zotero Desktop transaction, without enumeration, arbitrary JavaScript, or SQLite edits."
