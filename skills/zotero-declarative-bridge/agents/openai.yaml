interface:
  display_name: "Zotero Declarative Bridge"
  short_description: "Apply constrained existing-parent Zotero transactions"
  default_prompt: "Use $zotero-declarative-bridge to preview, apply, and read back this reviewed hash-bound Zotero Desktop transaction, including only the explicit shortTitle field operation when requested, without arbitrary JavaScript or SQLite edits."
